diff --git a/.opencode/plugin/jj-change-expander.ts b/.opencode/plugin/jj-change-expander.ts
index 44849b88e1..9803faf17c 100644
--- a/.opencode/plugin/jj-change-expander.ts
+++ b/.opencode/plugin/jj-change-expander.ts
@@ -1,4 +1,7 @@
 import { type Plugin } from "@opencode-ai/plugin";
+import { writeFileSync, mkdirSync } from "node:fs";
+import { tmpdir } from "node:os";
+import { join } from "node:path";
 
 /**
  * Expands jujutsu change references in messages.
@@ -6,11 +9,15 @@
  * Usage: Include %<change-id> in your message to auto-include the diff.
  * Example: "review %abc - check for performance issues"
  *
- * The diff will be added as context alongside your message.
+ * The diff will be written to a temp file and attached as a file part.
  */
 export const JjChangeExpanderPlugin: Plugin = async (ctx) => {
+  // Create a stable temp dir for diffs
+  const diffDir = join(tmpdir(), "opencode-jj-diffs");
+  mkdirSync(diffDir, { recursive: true });
+
   return {
-    "chat.message": async (input, output) => {
+    "chat.message": async (_input, output) => {
       // Find text parts in the message
       for (const part of output.parts) {
         if (part.type !== "text") continue;
@@ -21,9 +28,6 @@
 
         if (matches.length === 0) continue;
 
-        let expandedText = part.text;
-        const diffs: string[] = [];
-
         for (const match of matches) {
           const fullMatch = match[0];
           const changeId = match[1];
@@ -34,37 +38,32 @@
               .catch(() => null);
 
             if (diff && diff.trim()) {
-              // Remove the %changeId from text, we'll add diff as context
-              expandedText = expandedText.replace(
-                fullMatch,
-                `[change:${changeId}]`,
-              );
-              diffs.push(`<jujutsu-change id="${changeId}">
-<diff>
-${diff.trim()}
-</diff>
-</jujutsu-change>`);
+              // Write diff to temp file
+              const diffPath = join(diffDir, `${changeId}.diff`);
+              writeFileSync(diffPath, diff.trim());
+
+              part.text = part.text.replace(fullMatch, `[change:${changeId}]`);
+
+              // Attach temp file as a file part
+              output.parts.push({
+                id: crypto.randomUUID(),
+                sessionID: part.sessionID,
+                messageID: part.messageID,
+                type: "file",
+                mime: "text/plain",
+                filename: `${changeId}.diff`,
+                url: `file://${diffPath}`,
+              } as any);
             } else {
-              expandedText = expandedText.replace(
+              part.text = part.text.replace(
                 fullMatch,
                 `[no diff for change ${changeId}]`,
               );
             }
           } catch (error) {
-            expandedText = expandedText.replace(
-              fullMatch,
-              `[error: ${changeId}]`,
-            );
+            part.text = part.text.replace(fullMatch, `[error: ${changeId}]`);
           }
         }
-
-        // Update the text part
-        part.text = expandedText;
-
-        // Add diffs as additional context at the end
-        if (diffs.length > 0) {
-          part.text = `${expandedText}\n\n${diffs.join("\n\n")}`;
-        }
       }
     },
   };
diff --git a/docs/devlog/2026-01-01-jj-diff-file-attachment.patch b/docs/devlog/2026-01-01-jj-diff-file-attachment.patch
new file mode 100644
index 0000000000..e69de29bb2
