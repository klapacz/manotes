# Copy to .env.prod. Loaded by the worker deploy task with --stage prod.
IS_DEV=false
# Replace with a sender address authorized for Cloudflare Email Sending.
EMAIL_ADDRESS=noreply@example.com

BOOTSTRAP_ACCOUNT_EMAIL=you@example.com
