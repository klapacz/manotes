{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" },
    { "path": "./tsconfig.worker.json" }
  ],
  "compilerOptions": {
    // TODO: baseUrl and paths should not be here but are required for shadcn cli to work
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
